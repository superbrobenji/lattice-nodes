# ESP32/arduino-cli build artifacts
build/

# Superpowers subagent worktrees
.cave/

# legacy pre-restructure submodule path
main/lib/

# per-deployment master pubkey pin (generated via tools/gen_master_pubkey_pin.py)
firmware/main/config/master_pubkey_pin.h

# ESP-IDF component manager cache — regenerated by idf.py reconfigure, not source
/firmware/managed_components/
/firmware/dependencies.lock
/firmware/sdkconfig
/firmware/sdkconfig.old
